"""Shared Workbench core logic.

Code in this package is the stable seam between CLI scripts, the future local
API service, and browser-facing UI code.  Keep domain decisions here rather than
copying them into shell scripts or JavaScript.
"""
